# Repository-wide Copilot Instructions

- Whenever making significant changes, update `CHANGELOG.md` in the "Not released yet" section.
- Keep changelog entries brief (max 60 characters) and prefix each new entry with `<copilot>`.
